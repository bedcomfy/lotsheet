import BusesPage from "../components/mapp/BusesPage";

// Phone-shell tool (reached from the mobile tab bar). Works at any width but
// isn't linked from the desktop sidebar.
export default function Buses() {
  return <BusesPage />;
}
